apiVersion: v1
kind: Service
metadata:
  name: {{ include "card-transactions.fullname" . }}
  labels:
    {{- include "card-transactions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "card-transactions.selectorLabels" . | nindent 4 }}
