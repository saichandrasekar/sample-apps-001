apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "card-transactions.fullname" . }}-test-connection"
  labels:
    {{- include "card-transactions.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "card-transactions.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
